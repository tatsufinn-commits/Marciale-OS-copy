{
  "name": "mushoku-tensei-taskbar-hero",
  "version": "0.3.0.0",
  "description": "Build 20 — offline progression",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "terser": "^5.49.2",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "idb": "^8.0.0"
  }
}
