import test from 'node:test';import assert from 'node:assert/strict';import{ZoneContentSystem}from'../src/systems/ZoneContentSystem.js';test('zone system exposes stages and backgrounds from data',()=>{const s=new ZoneContentSystem({zones:[{id:'fittoa',stages:2,bgColors:{sky:'#fff'}}]});assert.equal(s.getStages('fittoa').length,2);assert.equal(s.getStages('fittoa')[0].unlocked,true);assert.equal(s.getBackground('fittoa').sky,'#fff')});