import test from'node:test';import assert from'node:assert/strict';import{ItemDatabase}from'../src/systems/ItemDatabase.js';test('database queries and rolls item templates',()=>{const d=new ItemDatabase({items:[{id:'s',slot:'weapon',rarity:'common'},{id:'r',slot:'ring',rarity:'rare'}]});assert.equal(d.get('s').id,'s');assert.equal(d.query({slot:'weapon'}).length,1);assert.equal(d.roll({rarity:'rare',rng:()=>0}).id,'r')});