import test from 'node:test'; import assert from 'node:assert/strict'; import { CraftingSystem } from '../src/systems/CraftingSystem.js';
class State {constructor(){this.data={player:{gold:30,manaDust:0}};}get(p){return structuredClone(p.split('.').reduce((o,k)=>o[k],this.data));}batch(_,c){c.forEach(x=>{const keys=x.path.split('.');const last=keys.pop();let o=this.data;keys.forEach(k=>o=o[k]);o[last]=x.value;});}}
test('craft spends resources only when inventory accepts output',()=>{const state=new State();const added=[];const craft=new CraftingSystem({stateManager:state,inventorySystem:{addItem:(id,q)=>{added.push([id,q]);return {added:true};}},recipes:[{id:'r',output:{itemId:'staff',quantity:1},cost:{gold:25}}]});assert.equal(craft.craft('r').crafted,true);assert.equal(state.data.player.gold,5);assert.equal(added[0][0],'staff');});
