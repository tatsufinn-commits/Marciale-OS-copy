import test from'node:test';import assert from'node:assert/strict';import{BossEncounter}from'../src/combat/BossEncounter.js';test('boss enters phase two below half health',()=>{const e=[];const b=new BossEncounter({eventBus:{emit:(...x)=>e.push(x)},events:{BOSS_PHASE_CHANGED:'phase'}});assert.equal(b.update({id:'boss',isBoss:true,phases:2,hp:40,maxHp:100}),2);assert.equal(e[0][0],'phase')});