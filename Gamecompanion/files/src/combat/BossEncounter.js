/** Build 18 boss encounter phase tracker, independent from specific boss art. */
export class BossEncounter { constructor({eventBus,events}={}){this.eventBus=eventBus;this.events=events;this.phase=1;} update(boss){if(!boss?.isBoss)return 1;const next=boss.hp/boss.maxHp<=.5&&boss.phases>=2?2:1;if(next!==this.phase){this.phase=next;this.eventBus?.emit(this.events.BOSS_PHASE_CHANGED,{bossId:boss.id,phase:next});}return this.phase;} createBoss(template,overrides={}){return {...template,...overrides,isBoss:true,phase:1,hp:overrides.hp??template.hp,maxHp:overrides.maxHp??template.hp};} }
