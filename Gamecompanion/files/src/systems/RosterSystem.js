/** Build 15 roster authority over data-driven playable character availability. */
export class RosterSystem { constructor({stateManager,templates=[]}={}){this.stateManager=stateManager;this.templates=new Map(templates.map(x=>[x.id,x]));} unlock(id){if(!this.templates.has(id))return false;this.stateManager.update('party.activeMembers',members=>members.includes(id)?members:[...members,id],{source:'roster'});return true;} getRoster(){const unlocked=this.stateManager.get('party.activeMembers');return [...this.templates.values()].map(t=>({...t,unlocked:unlocked.includes(t.id)}));} }
