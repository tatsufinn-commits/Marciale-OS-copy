/** Build 14 hero XP/level progression and skill unlock authority. */
export class ProgressionSystem { constructor({stateManager,eventBus,events}={}){Object.assign(this,{stateManager,eventBus,events});} xpRequired(level){return 20+level*15;} grantXp(amount){const hero=this.stateManager.get('combat.hero');let xp=(hero.xp??0)+amount,level=hero.level??1,levels=0;while(xp>=this.xpRequired(level)){xp-=this.xpRequired(level);level++;levels++;}const next={...hero,xp,level};this.stateManager.set('combat.hero',next,{source:'progression'});if(levels)this.eventBus?.emit(this.events.WEAVER_LEVEL_UP,{heroId:hero.id,level});return {level,xp,levels,nextRequired:this.xpRequired(level)};} }
